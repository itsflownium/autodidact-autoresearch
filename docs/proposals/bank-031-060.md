# Untested Proposal Bank 031-060

These proposals were generated against one frozen parent before protected training.
They are hypotheses, not experiment results or accepted improvements. The adaptive
autoresearch loop must revalidate or adapt each patch against its current parent.

- Frozen parent: `e150af023855c5bc3b0e9c8745c1126e8509258a`
- Training performed during generation: no
- Protected evaluation performed during generation: no
- Static gate: changed path, diff integrity, Python compilation, and parameter inspection

## 031. Raise gradient clipping threshold to 2.0

- **Hypothesis:** Raising the global gradient-norm clipping threshold to 2.0 will reduce excessive clipping during early high-gradient updates, improving optimization progress and lowering full-budget held-out BPB without destabilizing training.
- **Mechanism:** Larger gradients between norms 1.0 and 2.0 retain more of their intended AdamW update magnitude and direction, especially during early learning, while still bounding extreme updates.
- **Change:** In train.py, change the training CLI default --grad-clip from 1.0 to 2.0 while preserving the optimizer, learning-rate schedule, token budgets, architecture, interfaces, and 1,016,960-parameter count.
- **Expected BPB effect:** -0.004
- **Minimum useful gain:** 0.002
- **Failure signal:** The hypothesis is falsified if paired trials show no useful held-out BPB gain, increased loss or gradient instability, non-finite values, or controller resource-gate failure.
- **Resource risk:** No expected parameter, throughput, or peak-memory change; stability risk is slightly higher because fewer updates are strongly clipped.
- **Interaction risk:** The effect depends on peak learning rate, warmup duration, batch size, and architecture; it should be reassessed if queued proposals changing those settings are accepted.
- **Patch:** [`031.patch`](patches/031.patch)
- **Patch SHA-256:** `2efc7eaf1e8b96cf04b5b16949294925dd88d255fee8a3ff19309c16a683958d`
- **Static validation:** pass; 1016960 parameters
- **Experimental status:** queued, unmeasured

## 032. Learned causal prefix sink

- **Hypothesis:** A learned causal prefix shared across examples will reduce held-out BPB by giving attention a stable anchor and no-op sink, especially for randomly sampled training windows that begin without explicit preceding context.
- **Mechanism:** The prefix participates causally in every transformer layer, providing a trainable global conditioning vector and a location to absorb attention when no content token is useful, while preserving all content-to-content RoPE offsets and output shapes.
- **Change:** In train.py, add one learned 128-dimensional prefix embedding, prepend it to every input sequence before the transformer blocks, discard its output position before vocabulary projection, and update EXPECTED_PARAMETER_COUNT from 1,016,960 to 1,017,088.
- **Expected BPB effect:** -0.006
- **Minimum useful gain:** 0.003
- **Failure signal:** The candidate produces less than 0.003 BPB gain at full budget, regresses BPB, or violates throughput or memory limits.
- **Resource risk:** Adds 128 parameters and one internal sequence position, with an expected sub-1% increase in attention compute and activation memory. Syntax validation passed; runtime inspection was unavailable because the isolated environment could not resolve its dependency cache.
- **Interaction risk:** Learned absolute-position variants would need to account for the added internal prefix position; otherwise the patch is independent of the queued architecture and optimizer proposals.
- **Patch:** [`032.patch`](patches/032.patch)
- **Patch SHA-256:** `067a373138c11dca5eff35c5027d185b2c6eed17f0bb870187083f64fbf47b85`
- **Static validation:** pass; 1017088 parameters
- **Experimental status:** queued, unmeasured

## 033. Reset attention at story boundaries

- **Hypothesis:** Preventing attention across concatenated TinyStories boundaries will reduce training/evaluation context mismatch and lower full-budget held-out BPB by approximately 0.012.
- **Mechanism:** The end-of-text token begins a new attention segment, so prediction of the next story can use the delimiter but cannot exploit unrelated preceding-story tokens. Later tokens attend only within their own story segment.
- **Change:** In train.py, construct a causal same-story attention mask from end-of-text tokens for training batches and pass it through every Transformer block, preventing tokens from attending across story boundaries while preserving evaluation behavior, interfaces, dimensions, and the 1,016,960-parameter count.
- **Expected BPB effect:** -0.012
- **Minimum useful gain:** 0.005
- **Failure signal:** The candidate fails to improve held-out BPB by at least 0.005, or the explicit attention mask causes a controller throughput or peak-memory gate failure.
- **Resource risk:** Parameter-neutral, but each training batch creates a B×1×T×T boolean mask and uses masked scaled-dot-product attention; peak memory may rise modestly and throughput may regress if the backend loses its optimized causal fast path.
- **Interaction risk:** Partially overlaps proposal 18: boundary-target masking suppresses the next-story loss, whereas this patch retains that loss but removes preceding-story context. Combining them could obscure which boundary treatment caused an effect.
- **Patch:** [`033.patch`](patches/033.patch)
- **Patch SHA-256:** `14c791da791b20bc01ba7257d30086680fbd12bdb3d3b52ecb014e39f6b56ff6`
- **Static validation:** pass; 1016960 parameters
- **Experimental status:** queued, unmeasured

## 034. Post-activation MLP hidden normalization

- **Hypothesis:** Normalizing each MLP's post-GELU hidden activations will improve fixed-budget held-out BPB by stabilizing hidden-state scale and gradient flow across the four transformer blocks.
- **Mechanism:** Each added LayerNorm centers and rescales the expanded MLP representation before its residual output projection, limiting activation-scale drift and presenting the output projection with better-conditioned features while retaining learned per-channel scaling.
- **Change:** In train.py, insert a learned-scale, bias-free 512-channel LayerNorm between GELU and the output projection in every Transformer MLP, and update EXPECTED_PARAMETER_COUNT from 1,016,960 to 1,019,008.
- **Expected BPB effect:** -0.006
- **Minimum useful gain:** 0.003
- **Failure signal:** The paired full-budget BPB gain is below 0.003, optimization becomes less stable, or the four additional normalization operations cause a controller resource-gate regression.
- **Resource risk:** Adds 2,048 parameters and four normalization passes over 512-channel MLP activations; expect unchanged attention memory with a modest throughput reduction and small activation-memory increase. The resulting 1,019,008 parameters remain below the 1,050,000 cap.
- **Interaction risk:** The effect may depend on MLP activation or shape, especially the queued Squared-ReLU, SwiGLU, MLP-width, depth-for-width, and residual-stream normalization proposals; this patch is independently applicable to the supplied fixed parent.
- **Patch:** [`034.patch`](patches/034.patch)
- **Patch SHA-256:** `dd9c9fdf69239cbc3cf8966c097af86597b03829c361635ae586923cc8ffbe7e`
- **Static validation:** pass; 1019008 parameters
- **Experimental status:** queued, unmeasured

## 035. Bucketed learned relative attention bias

- **Hypothesis:** Learned per-head relative-distance priors alongside RoPE will lower held-out BPB because TinyStories contains recurring short-range syntactic and narrative dependencies that fixed rotary phases alone cannot specialize by layer and head.
- **Mechanism:** Each head learns a small layer-specific bias over exact nearby distances and logarithmically bucketed longer distances, allowing attention to favor useful relative spans without changing projections, residual paths, or token order.
- **Change:** In train.py, add 32-bucket learned causal relative-position biases independently for every attention head and layer, combine them with the causal attention mask alongside existing RoPE, and update EXPECTED_PARAMETER_COUNT from 1,016,960 to 1,017,472.
- **Expected BPB effect:** -0.008
- **Minimum useful gain:** 0.003
- **Failure signal:** The hypothesis is falsified if paired full-budget evaluation improves BPB by less than 0.003, worsens BPB, or the additive attention mask causes a controller-limit throughput or memory regression.
- **Resource risk:** Adds only 512 parameters, but materializing a dense per-layer attention bias may modestly increase peak memory and reduce throughput if it selects a slower scaled-dot-product-attention kernel.
- **Interaction risk:** Position-encoding proposals 2, 3, 13, and 26 could change or duplicate the positional signal, while proposal 33 could require composing its same-story mask with this additive causal bias. The patch is independently applicable to the supplied fixed parent.
- **Patch:** [`035.patch`](patches/035.patch)
- **Patch SHA-256:** `9522aa827e0dfc63241df1145da0bed54129209b3611b711f5332b5fe648236a`
- **Static validation:** pass; 1017472 parameters
- **Experimental status:** queued, unmeasured

## 036. Low-rank bigram logit shortcut

- **Hypothesis:** A low-rank direct bigram-logit shortcut will lower held-out BPB by at least 0.005 because frequent token-to-next-token transitions can be learned without consuming transformer capacity.
- **Mechanism:** The shortcut supplies a shallow token-conditioned correction for local lexical transitions while the transformer models longer-range context; zero-initializing its output preserves baseline logits at initialization.
- **Change:** In train.py, add a rank-9 token embedding and zero-initialized rank-9-to-vocabulary projection whose output is added to the transformer logits, and update EXPECTED_PARAMETER_COUNT from 1,016,960 to 1,049,216.
- **Expected BPB effect:** -0.012
- **Minimum useful gain:** 0.005
- **Failure signal:** A paired full-budget trial shows less than 0.005 BPB improvement, a BPB regression, or a material throughput or memory regression.
- **Resource risk:** Adds 32,256 parameters and a small rank-9 vocabulary projection at every token; peak parameter memory rises modestly and training throughput may decline slightly.
- **Interaction risk:** The added logit path may overlap with future output adapters, vocabulary biases, or logit-temperature changes and leaves only 784 parameters below the cap.
- **Patch:** [`036.patch`](patches/036.patch)
- **Patch SHA-256:** `99803d91bb204fd5a4450a9613a735c006a3bf3f03e54a1b0f00bc845891dba3`
- **Static validation:** pass; 1049216 parameters
- **Experimental status:** queued, unmeasured

## 037. Lower LayerNorm epsilon to 1e-6

- **Hypothesis:** Reducing LayerNorm epsilon to 1e-6 will improve held-out BPB by avoiding measurable under-normalization of the small-variance residual stream, especially early in training.
- **Mechanism:** With embeddings initialized at standard deviation 0.02, epsilon 1e-5 is non-negligible relative to initial feature variance. A tenfold-smaller epsilon should keep normalized activations closer to unit variance and slightly strengthen early attention and MLP learning signals.
- **Change:** In train.py, change ModelConfig.layer_norm_epsilon from 1e-5 to 1e-6; preserve all dimensions, interfaces, and the 1,016,960-parameter count.
- **Expected BPB effect:** -0.003
- **Minimum useful gain:** 0.002
- **Failure signal:** Paired trials show no BPB improvement of at least 0.002, or reveal increased gradient instability or non-finite values.
- **Resource risk:** No parameter, throughput, or meaningful peak-memory change is expected. Syntax compilation passed; the permitted inspect command was blocked by the environment's inaccessible uv cache.
- **Interaction risk:** The effect may weaken or change sign if combined with proposals that replace residual-stream LayerNorm or substantially alter residual magnitudes.
- **Patch:** [`037.patch`](patches/037.patch)
- **Patch SHA-256:** `eaed10f7467f1c67bf9f5753603109d288dbc848fb198a99372d953549b4b184`
- **Static validation:** pass; 1016960 parameters
- **Experimental status:** queued, unmeasured

## 038. Increase AdamW beta2 to 0.98

- **Hypothesis:** Increasing AdamW beta2 from 0.95 to 0.98 will lower full-budget held-out BPB by smoothing noisy squared-gradient estimates across the relatively small number of large-token batches.
- **Mechanism:** A longer second-moment averaging window should reduce step-size fluctuations between batches and provide more consistent per-parameter adaptive scaling while retaining beta1=0.9 responsiveness.
- **Change:** In train.py, change AdamW's second-moment coefficient in build_optimizer from beta2=0.95 to beta2=0.98; preserve all other optimizer settings, architecture, interfaces, token budgets, and the 1,016,960-parameter count.
- **Expected BPB effect:** -0.006
- **Minimum useful gain:** 0.003
- **Failure signal:** The candidate converges more slowly or produces no paired held-out BPB gain, indicating that the longer second-moment memory is too sluggish for the fixed training budgets.
- **Resource risk:** No parameter, memory, or meaningful throughput change is expected because AdamW retains the same optimizer states and operations.
- **Interaction risk:** The effect may depend on batch size, peak learning rate, and warmup schedule; it is independently applicable to the fixed parent but could overlap with queued optimizer and schedule proposals.
- **Patch:** [`038.patch`](patches/038.patch)
- **Patch SHA-256:** `688291ad7441ed0d091fe205766c6f7273268f30843a5c78f67e3bf02dbfa492`
- **Static validation:** pass; 1016960 parameters
- **Experimental status:** queued, unmeasured

## 039. Replace RoPE with fixed ALiBi

- **Hypothesis:** A fixed head-specific ALiBi locality prior will lower held-out BPB by at least 0.003 versus RoPE under the fixed budget because TinyStories relies heavily on short-range dependencies and the prior reduces the data needed to learn useful attention distances.
- **Mechanism:** Each attention head receives a different monotonic penalty for attending farther into the past, encouraging complementary local-to-broad receptive fields without adding parameters and retaining translation-invariant relative positioning.
- **Change:** In train.py, replace RoPE with parameter-free ALiBi: remove rope_base and rotary helpers, compute fixed head-specific linear distance slopes, and pass the resulting causal bias to scaled dot-product attention. Preserve all dimensions, interfaces, and the 1,016,960-parameter count.
- **Expected BPB effect:** -0.006
- **Minimum useful gain:** 0.003
- **Failure signal:** The candidate improves full-budget BPB by less than 0.003, regresses BPB, or breaches throughput or memory limits because the explicit attention bias prevents an efficient attention kernel.
- **Resource risk:** No parameter or optimizer-state increase. The per-layer causal bias is O(HL²), adding modest temporary memory and mask construction at context length 256; throughput may decline if the explicit mask changes scaled-attention kernel selection.
- **Interaction risk:** Conflicts with earlier positional-encoding proposals involving the RoPE base, learned absolute positions, partial RoPE, learned RoPE frequencies, or learned relative biases; otherwise independent, though changes to head count alter the ALiBi slope set.
- **Patch:** [`039.patch`](patches/039.patch)
- **Patch SHA-256:** `0883fac76d2fbaa95f834b834da2d8c086b783ea5671c9e525ecae62ac403eac`
- **Static validation:** pass; 1016960 parameters
- **Experimental status:** queued, unmeasured

## 040. Learned causal previous-token embedding mix

- **Hypothesis:** An explicit learnable previous-token pathway will improve held-out BPB by at least 0.005 by making local token transitions directly available before attention while retaining the baseline at initialization.
- **Mechanism:** A zero-initialized per-channel gate adds the preceding token embedding to each position before the transformer blocks. This supplies a cheap local bigram inductive bias while allowing training to select useful embedding channels without perturbing initial logits.
- **Change:** In train.py, add a zero-initialized learned 128-channel gate that mixes each position's immediately preceding token embedding into its residual-stream input, and update EXPECTED_PARAMETER_COUNT from 1,016,960 to 1,017,088.
- **Expected BPB effect:** -0.008
- **Minimum useful gain:** 0.005
- **Failure signal:** The paired full-budget trial shows less than 0.005 BPB improvement, the learned gate remains effectively zero, or throughput/memory exceeds controller limits.
- **Resource risk:** Adds 128 parameters and one shifted activation plus elementwise multiply-add; expected throughput impact is negligible, with a small activation-memory increase.
- **Interaction risk:** Low on the fixed parent; potentially redundant with a future accepted bigram-logit shortcut, token-shift mechanism, or input-embedding architecture change.
- **Patch:** [`040.patch`](patches/040.patch)
- **Patch SHA-256:** `346fe11b79144e8b6f4703c774c7d8ba7f1875c01a25d713bbb5da54528e34c0`
- **Static validation:** pass; 1017088 parameters
- **Experimental status:** queued, unmeasured

## 041. Wider 136-channel residual stream

- **Hypothesis:** Reallocating capacity toward the residual stream, tied token representation, and attention projections will improve held-out BPB more than the removed MLP hidden channels cost.
- **Mechanism:** The 136-channel residual stream increases token-embedding rank and attention capacity across every layer, while narrowing each MLP to 464 channels keeps the model below the parameter cap.
- **Change:** In train.py, increase ModelConfig.model_width from 128 to 136, reduce mlp_width from 512 to 464, and update EXPECTED_PARAMETER_COUNT from 1,016,960 to 1,045,704.
- **Expected BPB effect:** -0.012
- **Minimum useful gain:** 0.005
- **Failure signal:** Full-budget held-out BPB fails to improve by at least 0.005, or throughput or peak memory violates controller limits.
- **Resource risk:** Parameters increase by 2.83%; activation memory and matrix-multiplication cost should rise modestly, with a small possible throughput regression. Dependency-free syntax and parameter arithmetic checks passed; uv inspect could not run because uncached dependencies required unavailable network access.
- **Interaction risk:** Sensitive to later MLP-width, head-count, positional-encoding, or other width-allocation changes; independently applicable to the supplied fixed parent.
- **Patch:** [`041.patch`](patches/041.patch)
- **Patch SHA-256:** `80affb043e09dc755d450aa9bd555cf27a4f5a0ff0c8f04a8245593ada45a355`
- **Static validation:** pass; 1045704 parameters
- **Experimental status:** queued, unmeasured

## 042. Parallel attention and MLP residual branches

- **Hypothesis:** Parallel attention and MLP residual branches will lower held-out BPB by improving gradient conditioning and preventing each attention update from immediately shifting the same block's MLP input distribution.
- **Mechanism:** Both normalized branches now read the unchanged block input, shortening the residual computation graph and giving gradients two direct paths through each block while retaining the same learned transformations and residual sum.
- **Change:** In train.py, change TransformerBlock.forward so the attention and MLP branches both consume the same pre-block residual stream and their outputs are added in parallel; preserve all dimensions, interfaces, and the 1,016,960-parameter count.
- **Expected BPB effect:** -0.004
- **Minimum useful gain:** 0.002
- **Failure signal:** The full-budget paired trial shows no BPB gain or a regression, indicating that removing the within-block attention-to-MLP dependency costs more representation quality than the shorter optimization path provides.
- **Resource risk:** Parameter count and arithmetic are unchanged. Eager-mode throughput and peak memory should remain approximately neutral; compiled runtimes may expose limited branch parallelism.
- **Interaction risk:** Moderate interaction with future normalization, residual-scaling, depth, or branch-gating changes; independently applicable to the supplied fixed parent.
- **Patch:** [`042.patch`](patches/042.patch)
- **Patch SHA-256:** `e6d7bd5ef0349f57963851ae18edd6cfea3cf59a5817d079995098cb6c32dda4`
- **Static validation:** pass; 1016960 parameters
- **Experimental status:** queued, unmeasured

## 043. Unscaled residual projection initialization

- **Hypothesis:** For this shallow four-layer transformer, depth-scaled residual initialization is unnecessarily conservative, and larger initial residual projections will accelerate feature learning enough to lower held-out BPB.
- **Mechanism:** Attention and MLP residual output weights start with approximately 2.83 times greater standard deviation, strengthening early residual updates and gradient flow without changing model capacity.
- **Change:** In train.py, remove the depth-scaled reinitialization of each attention and MLP output projection, leaving the generic normal initialization with standard deviation 0.02 in effect; preserve all dimensions, interfaces, and the 1,016,960-parameter count.
- **Expected BPB effect:** -0.006
- **Minimum useful gain:** 0.002
- **Failure signal:** Matched-budget held-out BPB does not improve, or early loss and gradient norms become materially less stable.
- **Resource risk:** No parameter, activation-memory, or steady-state throughput change; larger early residual updates may increase optimization instability.
- **Interaction risk:** Initialization sensitivity depends on depth and branch topology, so interaction risk is highest with the queued depth-for-width and parallel-residual proposals.
- **Patch:** [`043.patch`](patches/043.patch)
- **Patch SHA-256:** `b1007e44dcd8665a3c53ce3db0c280255369de2f7f20119f6d8f58dcbfdbbd92`
- **Static validation:** pass; 1016960 parameters
- **Experimental status:** queued, unmeasured

## 044. Early context-length curriculum

- **Hypothesis:** A 64-to-256-token early context curriculum will lower held-out BPB by providing more frequent, lower-variance optimization updates while the model learns predominantly local TinyStories structure, before restoring full-context training for long-range dependencies.
- **Mechanism:** Shorter early sequences reduce attention work per example and increase the number of optimizer updates per token budget; linear growth then transfers training to the full 256-token context without changing evaluation behavior.
- **Change:** In train.py, add configurable training-context curriculum options defaulting to an initial length of 64 and a 20% curriculum ratio, then linearly grow sampled sequence length to the existing 256-token context during the first 20% of the token budget. Preserve evaluation context, architecture, interfaces, and the 1,016,960-parameter count.
- **Expected BPB effect:** -0.008
- **Minimum useful gain:** 0.005
- **Failure signal:** Paired full-budget trials fail to improve BPB by at least 0.005, show a persistent long-context evaluation penalty, or regress runtime because additional optimizer steps outweigh reduced early attention cost.
- **Resource risk:** Parameter count and eventual peak memory are unchanged. Early attention compute and activation memory decrease, but more optimizer launches may reduce throughput on devices with high per-step overhead.
- **Interaction risk:** Independent against the fixed parent, but effects may overlap with later batch-size changes and may depend on accepted learning-rate schedule or position-encoding patches.
- **Patch:** [`044.patch`](patches/044.patch)
- **Patch SHA-256:** `ee3f251ff7d743fa4764afe7972a9e4d0f28cca4f6aa41e47d0bf14dce032a52`
- **Static validation:** pass; 1016960 parameters
- **Experimental status:** queued, unmeasured

## 045. Factorized embeddings with MLP reallocation

- **Hypothesis:** A 96-dimensional vocabulary representation is sufficient for the 1,792-token vocabulary, so reallocating its redundant parameters to wider nonlinear MLPs will lower full-budget held-out BPB by at least 0.003.
- **Mechanism:** The factorized tied embedding removes 32,768 parameters from vocabulary interfaces and reallocates them exactly to four wider MLPs, increasing nonlinear feature capacity while preserving residual width, parameter count, and approximate multiply count.
- **Change:** In train.py, factor the tied token embedding/output space from 128 to 96 dimensions using variance-preserving input and output projections, increase each MLP width from 512 to 544, and preserve the 1,016,960-parameter count.
- **Expected BPB effect:** -0.008
- **Minimum useful gain:** 0.003
- **Failure signal:** Held-out BPB does not improve by at least 0.003, or the 96-dimensional output bottleneck causes slower convergence despite comparable throughput.
- **Resource risk:** Approximately compute-neutral; slightly larger MLP activations may marginally increase peak memory, while the narrower vocabulary projection offsets its arithmetic cost.
- **Interaction risk:** Conflicts with proposals that change embedding/output structure, MLP width, residual width, depth, or parameter-cap allocation; otherwise independently applicable to the fixed parent.
- **Patch:** [`045.patch`](patches/045.patch)
- **Patch SHA-256:** `bd4765b468c188f7453850623ce3abb9258c99ae819f2f57837be73ef892d321`
- **Static validation:** pass; 1016960 parameters
- **Experimental status:** queued, unmeasured

## 046. Hybrid local-global attention heads

- **Hypothesis:** Combining local and global heads will lower held-out BPB because local heads can specialize in short-range syntax and token patterns while global heads preserve story-level context.
- **Mechanism:** Half the heads receive an explicit 64-token causal mask, creating a mixed receptive field that adds a locality prior without reducing every layer's access to long-range context.
- **Change:** In train.py, configure two of four attention heads in every layer to use a 64-token causal sliding window while retaining two full-context causal heads; preserve RoPE, dimensions, interfaces, and the 1,016,960-parameter count.
- **Expected BPB effect:** -0.008
- **Minimum useful gain:** 0.003
- **Failure signal:** The candidate fails to improve full-budget held-out BPB by at least 0.003, or the split attention calls cause an unacceptable throughput or memory regression.
- **Resource risk:** Parameter-neutral with a small quadratic boolean mask, but splitting attention into global and local kernel calls may modestly reduce throughput; peak-memory impact should be small at context length 256.
- **Interaction risk:** Strong interaction with changes to head count, attention masks, positional encoding, or story-boundary attention; independently applicable to the supplied fixed parent.
- **Patch:** [`046.patch`](patches/046.patch)
- **Patch SHA-256:** `a0aba250b62d90c28979b7f2fcd2e311e981ab8307207271608f9942d29ff3c1`
- **Static validation:** pass; 1016960 parameters
- **Experimental status:** queued, unmeasured

## 047. Parameter-free causal MLP channel shift

- **Hypothesis:** A fixed one-token channel shift will improve held-out BPB by giving each MLP direct access to preceding-token features, reducing the optimization burden on attention for common local dependencies.
- **Mechanism:** Before each MLP, 25% of the normalized channels come from the immediately preceding position while 75% remain current-position channels, adding a parameter-free causal bigram inductive bias.
- **Change:** In train.py, causally shift the first quarter of normalized MLP input channels by one token position before every Transformer block's MLP, zero-filling the first position; preserve all dimensions, interfaces, and the 1,016,960-parameter count.
- **Expected BPB effect:** -0.004
- **Minimum useful gain:** 0.002
- **Failure signal:** The candidate produces no positive paired BPB gain, or the additional concatenations cause a material throughput or peak-memory regression.
- **Resource risk:** Parameter count is unchanged. Two small concatenations per block may slightly reduce throughput and increase activation memory, expected below 5% and 10% respectively.
- **Interaction risk:** Overlaps with earlier queued local-mixing proposals involving MLP/value convolutions or previous-token embedding mixing; it should be measured independently before combining with them.
- **Patch:** [`047.patch`](patches/047.patch)
- **Patch SHA-256:** `7909cae3202b6ffe3b41e8e6fd431c7f6dffa0a34587e6d444bca7e9a1851176`
- **Static validation:** pass; 1016960 parameters
- **Experimental status:** queued, unmeasured

## 048. Auxiliary two-token prediction head

- **Hypothesis:** A lightly weighted second-token prediction objective will encourage each causal hidden state to encode longer-range predictive features, reducing full-budget held-out BPB by about 0.008 without weakening the primary next-token objective.
- **Mechanism:** The auxiliary head receives the same final hidden states but has its own identity-initialized channel transform and normalization. Predicting targets shifted by one additional position supplies denser future-oriented supervision while the normal tied output head and primary cross-entropy remain authoritative.
- **Change:** In train.py, add an identity-initialized 128-by-128 auxiliary projection and weight-only normalization that predict the token two positions ahead through the tied vocabulary output during training, add that auxiliary cross-entropy at weight 0.1, and update EXPECTED_PARAMETER_COUNT from 1,016,960 to 1,033,472. Primary logits, evaluation, and generation remain unchanged.
- **Expected BPB effect:** -0.008
- **Minimum useful gain:** 0.005
- **Failure signal:** The hypothesis is falsified if paired full-budget trials improve held-out BPB by less than 0.005, regress BPB, or breach controller throughput or peak-memory limits.
- **Resource risk:** Parameter count rises by 16,512 but remains below the cap. Training adds one 128-by-128 projection and a second tied-vocabulary projection over all but the last position, so throughput may decline roughly 15–25% and peak activation memory may increase materially; evaluation and generation costs are unchanged.
- **Interaction risk:** The patch is independently applicable to the fixed parent but may interact with proposals that alter the output space or loss, especially logit z-loss, vocabulary bias, output adapters, factorized embeddings, and the bigram shortcut.
- **Patch:** [`048.patch`](patches/048.patch)
- **Patch SHA-256:** `178395ae364881c2c756f32cb40d7c821c5592c615aae119ddb7a856ff577277`
- **Static validation:** pass; 1033472 parameters
- **Experimental status:** queued, unmeasured

## 049. Per-layer token-embedding reinjection

- **Hypothesis:** Giving each depth a learnable zero-initialized route to the current token embedding will improve held-out BPB by at least 0.002 by allowing later blocks to recover lexical identity without preserving it through every residual transformation.
- **Mechanism:** Per-channel deep skips selectively restore token-identity features and shorten gradient paths to the tied embedding matrix, while zero initialization exactly preserves the parent function at initialization.
- **Change:** In train.py, add a zero-initialized learned [4, 128] table of per-layer, per-channel embedding-reinjection scales; after each Transformer block, add the scaled original token embedding to the residual stream, and update EXPECTED_PARAMETER_COUNT from 1,016,960 to 1,017,472.
- **Expected BPB effect:** -0.004
- **Minimum useful gain:** 0.002
- **Failure signal:** Paired full-budget held-out BPB improves by less than 0.002, or throughput, memory, or stability gates regress.
- **Resource risk:** Adds 512 parameters (0.05%) and one broadcast multiply/add per layer; expected throughput and peak-memory effects are negligible. Read-only AST validation passed; runtime inspection was unavailable because the sandbox blocked uv's external cache and system Python lacked project dependencies.
- **Interaction risk:** Low against the fixed parent, but potentially redundant with later acceptance of residual-scaling proposal 25 or previous-token embedding-mixing proposal 40.
- **Patch:** [`049.patch`](patches/049.patch)
- **Patch SHA-256:** `b64a93ddd429082fbb88f7d3167100a365c05e4797c4702493a7a76bda2d12c4`
- **Static validation:** pass; 1017472 parameters
- **Experimental status:** queued, unmeasured

## 050. Token-embedding dropout

- **Hypothesis:** Applying 10% dropout only at the residual-stream input will reduce token-identity co-adaptation and modestly improve full-budget held-out BPB without the optimization cost of attention or residual-branch dropout.
- **Mechanism:** Randomly masking and rescaling embedding channels during training forces downstream blocks to distribute lexical evidence across features, while automatic dropout disabling leaves evaluation and generation deterministic and unchanged.
- **Change:** In train.py, add ModelConfig.embedding_dropout defaulting to 0.1, validate its range, and apply parameter-free dropout once to token embeddings before the Transformer blocks. Preserve evaluation behavior, interfaces, dimensions, and the 1,016,960-parameter count.
- **Expected BPB effect:** -0.006
- **Minimum useful gain:** 0.003
- **Failure signal:** The candidate fails to improve full-budget held-out BPB by at least 0.003, or shows materially slower training or unstable optimization relative to the paired parent.
- **Resource risk:** No parameter or persistent-memory increase; one embedding-sized dropout mask per training batch may cause a small throughput and transient-memory regression. Focused syntax checks passed, but inspect could not run because the isolated environment lacked cached dependencies and network access.
- **Interaction risk:** The effect may overlap with future regularization, context-curriculum, or embedding-reinjection patches; it is otherwise independently applicable to the fixed parent.
- **Patch:** [`050.patch`](patches/050.patch)
- **Patch SHA-256:** `8c5689074ee87f48f5e2393e112bb25d743a65f42d3bee2cc7f4bc8c51a40122`
- **Static validation:** pass; 1016960 parameters
- **Experimental status:** queued, unmeasured

## 051. Order-one query-key logit initialization

- **Hypothesis:** Order-one initial query-key dot-product variance will help attention learn useful token and position selectivity sooner, reducing held-out BPB under the fixed token budget.
- **Mechanism:** The baseline's 0.02 Q/K initialization produces nearly uniform initial attention logits with roughly 0.05 standard deviation. Xavier-scale Q/K initialization raises logits to approximately order-one variance without enlarging values or residual outputs, strengthening early attention-gradient signals while leaving Q/K norms learnable.
- **Change:** In train.py, initialize only the query and key slices of every packed QKV projection with standard deviation 1/sqrt(model_width), while retaining the existing 0.02 value initialization and depth-scaled residual-output initialization; preserve dimensions, interfaces, and the 1,016,960-parameter count.
- **Expected BPB effect:** -0.006
- **Minimum useful gain:** 0.002
- **Failure signal:** Protected paired trials show less than 0.002 BPB gain, unstable loss or gradients, or a resource-gate regression.
- **Resource risk:** No parameter, activation-memory, or steady-state throughput change is expected; the main risk is optimization instability from initially sharper attention. Dependency-free AST validation passed, while runtime inspection was unavailable because uv's cache was sandbox-inaccessible and system Python lacked numpy.
- **Interaction risk:** Query-key RMS normalization would largely neutralize the initialization effect; learned attention temperatures and alternative positional-attention patches may also interact.
- **Patch:** [`051.patch`](patches/051.patch)
- **Patch SHA-256:** `71f378286b6b34fb94b1b7b017495ac64c866a392a66f0816328d70060467e21`
- **Static validation:** pass; 1016960 parameters
- **Experimental status:** queued, unmeasured

## 052. Per-token tied-output logit scales

- **Hypothesis:** Per-token output scaling will lower held-out BPB by decoupling each token's output-logit magnitude from the norm its tied embedding requires for input representation.
- **Mechanism:** Each zero-initialized log scale starts as an exact identity transformation, then learns a positive token-specific classifier scale. This preserves tied input/output directions while allowing frequency- and token-dependent output calibration without altering hidden dimensions.
- **Change:** In train.py, add a zero-initialized learned vocabulary-sized output_log_scales parameter, multiply each tied-output logit by exp(output_log_scales), and update EXPECTED_PARAMETER_COUNT from 1,016,960 to 1,018,752.
- **Expected BPB effect:** -0.006
- **Minimum useful gain:** 0.003
- **Failure signal:** The paired full-budget candidate improves BPB by less than 0.003, worsens BPB, produces unstable logit scales or non-finite values, or causes a material throughput or memory regression.
- **Resource risk:** Adds 1,792 parameters and one broadcast exponential/vector multiplication over logits. Expected peak-memory and throughput impact is negligible, with parameter count remaining well below the 1,050,000 cap.
- **Interaction risk:** The benefit may be reduced by accepted patches that untie or augment the output head, add vocabulary-logit biases, or otherwise provide token-specific output calibration; it is independently applicable to the supplied fixed parent.
- **Patch:** [`052.patch`](patches/052.patch)
- **Patch SHA-256:** `8bbbebb28a848f52d659c89839cbcc26d3992a5e39a03bd3fe03a2ac3b38a420`
- **Static validation:** pass; 1018752 parameters
- **Experimental status:** queued, unmeasured

## 053. Strided midpoint deep supervision

- **Hypothesis:** Strided next-token supervision at the model midpoint will improve full-budget held-out BPB by strengthening gradients to the lower blocks during the small number of optimizer steps available.
- **Mechanism:** The auxiliary objective encourages the lower half of the network to form directly predictive representations while reusing existing normalization and output weights; striding limits its compute and activation-memory cost.
- **Change:** In train.py, add a parameter-free midpoint next-token auxiliary loss: after the second of four Transformer blocks, apply the shared final normalization and tied output projection to every fourth position, then add its cross-entropy to the primary training loss at weight 0.1. Evaluation, generation, dimensions, and the 1,016,960-parameter count remain unchanged.
- **Expected BPB effect:** -0.01
- **Minimum useful gain:** 0.005
- **Failure signal:** The candidate produces no positive paired BPB gain, destabilizes optimization, or exceeds throughput or peak-memory limits.
- **Resource risk:** No parameter or inference overhead; training adds an output projection and cross-entropy over one quarter of positions, plausibly reducing throughput by 5–10% and adding roughly one-quarter of the primary-logit activation memory.
- **Interaction risk:** The effect may overlap with the queued auxiliary two-token prediction head and may change with accepted depth, factorized-output, or untied-output variants.
- **Patch:** [`053.patch`](patches/053.patch)
- **Patch SHA-256:** `b18f65d3a832ae69c0c436911eee694f83e8cc5a9601299af3c3291f660cb431`
- **Static validation:** pass; 1016960 parameters
- **Experimental status:** queued, unmeasured

## 054. Sandwich half-width MLP sublayers

- **Hypothesis:** Distributing the same MLP capacity around attention will improve held-out BPB by adding nonlinear processing depth before and after contextual mixing without increasing parameters or matrix-multiplication work.
- **Mechanism:** Each block gains a nonlinear feature transformation before attention while retaining one afterward. Halving both hidden widths keeps total MLP parameters and arithmetic approximately constant, while the extra residual stage increases effective depth.
- **Change:** In train.py, split every 512-channel GELU MLP into two parameter-neutral 256-channel GELU MLPs: one residual sublayer before attention and one after attention, sharing the existing MLP normalization. Preserve interfaces and the 1,016,960-parameter count.
- **Expected BPB effect:** -0.012
- **Minimum useful gain:** 0.005
- **Failure signal:** The paired full-budget candidate shows no BPB improvement, or the additional sublayer launches cause a material throughput regression.
- **Resource risk:** Parameter count and main GEMM FLOPs remain unchanged; extra normalization, activation, and residual-add launches may slightly reduce throughput and modestly increase activation memory.
- **Interaction risk:** High interaction with proposals that alter MLP activation, width, normalization, residual ordering, or initialization; independently applicable to the fixed parent.
- **Patch:** [`054.patch`](patches/054.patch)
- **Patch SHA-256:** `f5093bc7593a2832bdb1647449ec1a47464e8b419e429f4234412478e7d009c8`
- **Static validation:** pass; 1016960 parameters
- **Experimental status:** queued, unmeasured

## 055. Per-channel intermediate-depth readout mixing

- **Hypothesis:** Allowing the output head to recover useful channel features from earlier Transformer depths will reduce held-out BPB by at least 0.003 compared with relying exclusively on the fourth-block state.
- **Mechanism:** A zero-initialized 3-by-128 parameter preserves baseline outputs and gradients at initialization, then learns a per-channel affine mixture between the final state and each earlier block state, providing direct access to lexical and lower-level features that the last block may over-transform.
- **Change:** In train.py, add zero-initialized learned per-channel mixing coefficients for the first three Transformer block outputs and use them to interpolate those intermediate states into the final residual stream before final normalization; update EXPECTED_PARAMETER_COUNT from 1,016,960 to 1,017,344.
- **Expected BPB effect:** -0.006
- **Minimum useful gain:** 0.003
- **Failure signal:** The hypothesis is falsified if paired full-budget evaluation shows less than 0.003 BPB gain, worse BPB, instability, or a material throughput or peak-memory regression.
- **Resource risk:** Adds 384 parameters and three elementwise residual-mixing operations. Expected throughput impact is below 1%, with a small activation-memory increase and no change to attention complexity or context length.
- **Interaction risk:** Independently applicable to the fixed parent, but its effect may depend on later depth, residual-scaling, parallel-branch, or embedding-reinjection changes such as proposals 16, 25, 42, and 49.
- **Patch:** [`055.patch`](patches/055.patch)
- **Patch SHA-256:** `f4f0ab2a3302c54e937bfc181241d4d01d23a11082847fd4ee12f4525cffdcf7`
- **Static validation:** pass; 1017344 parameters
- **Experimental status:** queued, unmeasured

## 056. Zero-initialized residual output projections

- **Hypothesis:** Initializing every Transformer block as an exact identity map will lower held-out BPB by eliminating early random residual-stream perturbations and letting residual branches enter training gradually.
- **Mechanism:** Zero output projections initially suppress attention and MLP branch outputs. Their output weights learn on the first update, after which gradients reach branch internals, creating a short data-driven warm start without architectural changes.
- **Change:** In train.py, zero-initialize every attention output projection and MLP output projection, replacing depth-scaled random residual initialization while preserving all dimensions, interfaces, and the 1,016,960-parameter count. Static syntax compilation passed; runtime inspection was unavailable because sandboxed dependencies could not be downloaded.
- **Expected BPB effect:** -0.004
- **Minimum useful gain:** 0.003
- **Failure signal:** Matched-budget trials show no held-out BPB reduction, persistent slower loss reduction, or ineffective upstream branch learning.
- **Resource risk:** No material steady-state throughput or peak-memory impact. The principal risk is a brief optimization delay because branch-internal parameters receive zero gradient on the first update.
- **Interaction risk:** Directly conflicts with proposal 43 and may interact with residual-gating or residual-scale variants; independently applicable to the fixed parent.
- **Patch:** [`056.patch`](patches/056.patch)
- **Patch SHA-256:** `f5430128f28546df4bfd271f351a11e62230915938cceb5085a227b3723262d5`
- **Static validation:** pass; 1016960 parameters
- **Experimental status:** queued, unmeasured

## 057. Parameter-free query-gated attention

- **Hypothesis:** Identity-centered query gating will lower held-out BPB by letting each token selectively suppress or amplify retrieved attention features without consuming parameter budget.
- **Mechanism:** The shared query projection now controls both attention selection and a bounded, channel-wise output gate. This adds a multiplicative token-conditioned interaction while starting near unit scale, so baseline signal magnitude is initially preserved.
- **Change:** In train.py, derive an identity-centered parameter-free gate as 2*sigmoid(query) immediately after splitting attention heads and before RoPE, then multiply each scaled-dot-product attention output by that gate before merging heads. Preserve all dimensions, interfaces, and the 1,016,960-parameter count.
- **Expected BPB effect:** -0.004
- **Minimum useful gain:** 0.002
- **Failure signal:** The hypothesis is falsified if paired full-budget trials show less than 0.002 BPB gain, or if the added sigmoid materially regresses throughput, peak memory, or training stability.
- **Resource risk:** Parameter count is unchanged. The extra sigmoid, multiplication, and retained query-sized gate may cause a small throughput reduction and modest activation-memory increase; syntax compilation passed, while the allowed inspect command was blocked by the sandboxed uv cache.
- **Interaction risk:** The gate is independently applicable to the fixed parent, but its behavior may interact with query-key RMS normalization or altered query initialization because both change the pre-RoPE query distribution driving the sigmoid.
- **Patch:** [`057.patch`](patches/057.patch)
- **Patch SHA-256:** `7c1f16d402bffa78ec8eea6d78d48ce5ba48d98d720b6c665a6e40c3103a093a`
- **Static validation:** pass; 1016960 parameters
- **Experimental status:** queued, unmeasured

## 058. Width-scaled token embeddings

- **Hypothesis:** Scaling the initial token embeddings by sqrt(model_width) will improve held-out BPB by preserving token identity at a magnitude better balanced with the first residual branch updates.
- **Mechanism:** The parameter-free scale increases the initial residual-stream magnitude from the 0.02 embedding initialization, reducing early overwriting by attention and MLP residual updates while pre-normalization keeps branch inputs well-conditioned.
- **Change:** In train.py, multiply token embeddings by sqrt(model_width) before entering the Transformer blocks; preserve all dimensions, interfaces, and the 1,016,960-parameter count.
- **Expected BPB effect:** -0.008
- **Minimum useful gain:** 0.004
- **Failure signal:** The candidate fails to improve held-out BPB by at least 0.004, or shows a material throughput, memory, or stability regression.
- **Resource risk:** Negligible parameter and compute impact; one elementwise scaling operation may add a small transient activation allocation, with no expected measurable throughput or peak-memory regression.
- **Interaction risk:** Most sensitive to accepted changes that alter embedding dimensionality, embedding initialization, embedding dropout, or residual-branch initialization; independently applicable to the supplied fixed parent.
- **Patch:** [`058.patch`](patches/058.patch)
- **Patch SHA-256:** `43d0214506fa7a29d57f61608d9295adcbf372ca025b46772fb32e23bf32b286`
- **Static validation:** pass; 1016960 parameters
- **Experimental status:** queued, unmeasured

## 059. SiLU transformer MLP activation

- **Hypothesis:** SiLU's smooth, non-monotonic gating will improve feature selection and gradient flow enough to reduce full-budget held-out BPB relative to GELU under the fixed training budget.
- **Mechanism:** SiLU continuously scales each hidden feature by its sigmoid activation, retaining small negative responses while suppressing weak inputs; this changes MLP optimization and representation sparsity without adding parameters or altering residual geometry.
- **Change:** In train.py, replace the GELU activation in every Transformer MLP with parameter-free SiLU while preserving all dimensions, interfaces, initialization behavior, and the 1,016,960-parameter count.
- **Expected BPB effect:** -0.003
- **Minimum useful gain:** 0.002
- **Failure signal:** Held-out BPB is unchanged or worse than the paired parent, or training throughput regresses materially despite identical parameter count and tensor shapes.
- **Resource risk:** No parameter or activation-memory increase; throughput should remain approximately neutral, with only backend-dependent differences between SiLU and GELU kernels.
- **Interaction risk:** Directly conflicts with earlier queued activation proposals using Squared-ReLU or SwiGLU, but is independently applicable to the fixed parent and does not depend on other queued patches.
- **Patch:** [`059.patch`](patches/059.patch)
- **Patch SHA-256:** `67af4c86492d5298a7d372584ae7e99c8e6cb6b29084f8b49e9031315c4bc37e`
- **Static validation:** pass; 1016960 parameters
- **Experimental status:** queued, unmeasured

## 060. Hybrid RoPE and position-free attention heads

- **Hypothesis:** A 50/50 mixture of rotary and position-free attention heads will improve held-out BPB by at least 0.003 by allowing specialization between order-sensitive and content-based retrieval.
- **Mechanism:** Two heads retain relative-position phase information while two avoid rotary phase distortion, providing complementary position-aware and position-invariant attention channels without adding parameters.
- **Change:** In train.py, add ModelConfig.num_rope_heads=2 and apply RoPE only to the first two of four attention heads, leaving the other two position-free; preserve dimensions, interfaces, and the 1,016,960-parameter count.
- **Expected BPB effect:** -0.008
- **Minimum useful gain:** 0.003
- **Failure signal:** Held-out BPB is unchanged or worse, or position-free heads degrade optimization stability or order-sensitive predictions.
- **Resource risk:** Parameter-neutral with negligible concatenation overhead and essentially unchanged peak memory. Static syntax parsing passed; train.py inspect could not run because the sandbox denied access to uv's cache.
- **Interaction risk:** Changes to head count, RoPE, positional biases, or per-head frequency learning may overlap with or alter the intended specialization.
- **Patch:** [`060.patch`](patches/060.patch)
- **Patch SHA-256:** `f21d3ac22e86e7047207810f9ff1dec8a362a65a88c28cbfcac80d972edab352`
- **Static validation:** pass; 1016960 parameters
- **Experimental status:** queued, unmeasured
