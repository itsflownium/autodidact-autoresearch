# Untested Proposal Bank 001-030

These proposals were generated against one frozen parent before protected training.
They are hypotheses, not experiment results or accepted improvements. The adaptive
autoresearch loop must revalidate or adapt each patch against its current parent.

- Frozen parent: `e150af023855c5bc3b0e9c8745c1126e8509258a`
- Training performed during generation: no
- Protected evaluation performed during generation: no
- Static gate: changed path, diff integrity, Python compilation, and parameter inspection

## 001. Squared-ReLU transformer MLP

- **Hypothesis:** Replacing GELU with squared ReLU will lower full-budget held-out BPB by approximately 0.008 because the sharper, superlinear positive-feature response should improve the expressivity and optimization efficiency of the fixed-width MLP.
- **Mechanism:** Squared ReLU keeps negative features gated while amplifying confident positive features, increasing the effective nonlinearity of each parameter-neutral feed-forward layer without changing attention, data order, or optimizer behavior.
- **Change:** In train.py, add a parameter-free SquaredReLU module implementing square(ReLU(x)) and replace the Transformer MLP's GELU activation with it; preserve all dimensions and the 1,016,960-parameter count.
- **Expected BPB effect:** -0.008
- **Minimum useful gain:** 0.003
- **Failure signal:** The hypothesis is falsified if paired held-out BPB gain is below 0.003, or if activation growth causes non-finite values, materially worse throughput, or increased peak memory.
- **Resource risk:** No parameter or persistent-memory increase. ReLU plus squaring should have similar compute cost to GELU, with a small risk of larger activation or gradient magnitudes.
- **Interaction risk:** Low against the fixed parent because no previous proposals are queued; later changes to MLP width, initialization, normalization, or learning rate could alter squared ReLU's activation scale and effect.
- **Patch:** [`001.patch`](patches/001.patch)
- **Patch SHA-256:** `5ff11214770ebbee688644c8853b8bd163a835497bc4825e047b8a2b546c5cda`
- **Static validation:** pass; 1016960 parameters
- **Experimental status:** queued, unmeasured

## 002. Short-context RoPE base 1,000

- **Hypothesis:** At the fixed 256-token context length, lowering the RoPE base to 1,000 will improve held-out BPB by giving more head dimensions position-varying phases over the usable context window.
- **Mechanism:** The lower base increases angular variation in RoPE's slower frequency bands, improving resolution of medium- and long-range token offsets within 256-token sequences without adding parameters.
- **Change:** In train.py, change ModelConfig.rope_base from 10,000.0 to 1,000.0; preserve all dimensions, interfaces, and the 1,016,960-parameter count.
- **Expected BPB effect:** -0.008
- **Minimum useful gain:** 0.003
- **Failure signal:** Held-out BPB does not improve by at least 0.003, or optimization stability or throughput regresses beyond controller limits.
- **Resource risk:** Parameter- and activation-memory-neutral; negligible throughput impact because the same RoPE operations and tensor shapes are retained.
- **Interaction risk:** Independent of the queued Squared-ReLU proposal, though their combined BPB effects may not be additive if both alter how blocks use positional features.
- **Patch:** [`002.patch`](patches/002.patch)
- **Patch SHA-256:** `dc6d8d8aab2311f9c2bd29df48c8be8fa8de223dd35eceecd8d50eee91636336`
- **Static validation:** pass; 1016960 parameters
- **Experimental status:** queued, unmeasured

## 003. Learned absolute position embeddings

- **Hypothesis:** For fixed 256-token TinyStories contexts, learned absolute position vectors will improve held-out BPB by at least 0.005 versus RoPE because the model can directly fit position-specific structure while using the remaining parameter allowance.
- **Mechanism:** The additional 32,768 learned values provide explicit, trainable position features and remove the need for the small model to express fixed-context positional regularities through rotary attention geometry alone.
- **Change:** In train.py, replace parameter-free rotary position encoding with a learned 256-by-128 absolute position embedding, add it to token embeddings, remove the obsolete RoPE code/configuration, and update the enforced parameter count from 1,016,960 to 1,049,728 (within the 1,050,000 cap).
- **Expected BPB effect:** -0.012
- **Minimum useful gain:** 0.005
- **Failure signal:** The candidate improves held-out BPB by less than 0.005, regresses BPB, or breaches controller throughput, memory, or stability gates.
- **Resource risk:** Adds 32,768 parameters (3.2%) and small parameter, gradient, optimizer-state, and checkpoint memory overhead. Compute should remain roughly neutral because one embedding lookup replaces per-layer rotary transformations.
- **Interaction risk:** Mutually exclusive with queued proposal 2, which changes the RoPE base; largely orthogonal to the queued Squared-ReLU proposal.
- **Patch:** [`003.patch`](patches/003.patch)
- **Patch SHA-256:** `1febc18a6075b7581f709dcfeb40ce77ea331272578b70c403a675da109b0936`
- **Static validation:** pass; 1049728 parameters
- **Experimental status:** queued, unmeasured

## 004. Parameter-free query-key RMS normalization

- **Hypothesis:** Parameter-free per-head query-key RMS normalization will lower full-budget held-out BPB by at least 0.005 by preventing poorly scaled attention logits during early and later training.
- **Mechanism:** Normalizing query and key vectors to consistent per-head RMS makes scaled dot-product logits less sensitive to projection magnitude drift, yielding more stable attention entropy and gradients while retaining directional information.
- **Change:** In train.py, RMS-normalize each attention head's query and key vectors across head_dim immediately after splitting heads and before applying RoPE; preserve all dimensions, interfaces, and the 1,016,960-parameter count.
- **Expected BPB effect:** -0.012
- **Minimum useful gain:** 0.005
- **Failure signal:** The hypothesis is falsified if paired trials show less than 0.005 BPB gain, a BPB regression, instability, or a throughput or peak-memory regression beyond controller limits.
- **Resource risk:** No parameter increase. Two additional per-head reductions and normalizations may slightly reduce throughput and retain modest extra query/key activations, so runtime and peak memory require controller measurement.
- **Interaction risk:** Low. RMS normalization is independent of the queued MLP activation change and learned absolute-position proposal; because RoPE is norm-preserving, normalization before RoPE should also remain compatible with the queued RoPE-base change.
- **Patch:** [`004.patch`](patches/004.patch)
- **Patch SHA-256:** `643ca6e1962404ea581b848533ffc242ea1551b9ef5ac2d8d382ac8f04650924`
- **Static validation:** pass; 1016960 parameters
- **Experimental status:** queued, unmeasured

## 005. Residual-stream RMSNorm

- **Hypothesis:** RMSNorm will improve full-budget held-out BPB by at least 0.005 because retaining each residual stream's mean while controlling its RMS will preserve useful signal and stabilize scale throughout this small pre-normalized transformer.
- **Mechanism:** Each normalization site rescales hidden states by their root-mean-square without subtracting the feature mean, maintaining residual-stream mean information while bounding activation magnitude and gradient scale.
- **Change:** In train.py, replace the learned-scale, bias-free LayerNorm used before attention, before the MLP, and after the final block with parameter-neutral RMSNorm; preserve all dimensions, state-dict parameter names, interfaces, and the 1,016,960-parameter count.
- **Expected BPB effect:** -0.008
- **Minimum useful gain:** 0.005
- **Failure signal:** The candidate fails to reduce held-out BPB by at least 0.005, becomes unstable, or incurs a material throughput or peak-memory regression.
- **Resource risk:** Parameter count and persistent memory are unchanged. The explicit reduction and elementwise operations may be less fused than LayerNorm on some devices, causing a small throughput or temporary-memory regression.
- **Interaction risk:** The benefit may overlap with queued query-key RMS normalization if that patch is later accepted; the queued activation and positional-encoding variants do not directly conflict, although their changed activation statistics could alter the effect size.
- **Patch:** [`005.patch`](patches/005.patch)
- **Patch SHA-256:** `19c72d28b8ff41e92efcea48e85ee1a63ebdf20aba88e908716580e630cd93e1`
- **Static validation:** pass; 1016960 parameters
- **Experimental status:** queued, unmeasured

## 006. Near-parameter-matched SwiGLU MLP

- **Hypothesis:** A near-parameter-matched SwiGLU feed-forward sublayer will lower held-out BPB by learning input-dependent feature gating.
- **Mechanism:** SiLU-activated gates multiplicatively select value features while retaining essentially the same projection parameter budget.
- **Change:** In train.py, replace each 512-channel GELU MLP with a near-parameter-matched SwiGLU MLP using two 341-channel gate/value branches, and update the enforced parameter count from 1,016,960 to 1,016,448.
- **Expected BPB effect:** -0.012
- **Minimum useful gain:** 0.003
- **Failure signal:** Paired held-out BPB gain is below 0.003, or stability or throughput materially regresses.
- **Resource risk:** Trainable parameters decrease by 512; compute and peak activation memory should remain approximately unchanged, with a small possible elementwise throughput cost.
- **Interaction risk:** Mutually exclusive with the queued Squared-ReLU MLP proposal; normalization changes may also alter gating dynamics.
- **Patch:** [`006.patch`](patches/006.patch)
- **Patch SHA-256:** `f51d7e735308ece8ca009ace4a7639ec59a87901cac634b480b974c9ad26cdf2`
- **Static validation:** pass; 1016448 parameters
- **Experimental status:** queued, unmeasured

## 007. No weight decay on tied embeddings

- **Hypothesis:** Excluding the tied token-embedding/output matrix from decoupled weight decay will improve full-budget held-out BPB by preserving token representations and output-logit scale, especially for less frequent tokens.
- **Mechanism:** The tied matrix serves simultaneously as the input lookup table and output classifier. Avoiding AdamW shrinkage prevents uniform erosion of its row norms while retaining regularization on attention and MLP matrices.
- **Change:** In train.py, update build_optimizer so nn.Embedding weights—including the tied token-embedding/output matrix—are assigned to AdamW's zero-weight-decay group while all other matrix weights retain the configured decay; preserve the architecture, interfaces, and 1,016,960-parameter count.
- **Expected BPB effect:** -0.008
- **Minimum useful gain:** 0.005
- **Failure signal:** Paired evaluation shows no held-out BPB improvement, or reduced embedding decay increases logit norms without improving held-out negative log-likelihood.
- **Resource risk:** No parameter or activation-memory increase and negligible throughput impact; optimizer state size is unchanged because the same tensor merely moves between existing parameter groups.
- **Interaction risk:** If learned absolute position embeddings from proposal 3 are later adopted, the generic embedding exclusion would also remove decay from those weights and should be narrowed or revalidated; the patch is otherwise independent of the queued architecture proposals.
- **Patch:** [`007.patch`](patches/007.patch)
- **Patch SHA-256:** `30767fc677d92d6593ce8253d8e223035549813480172b92151bf1b896224546`
- **Static validation:** pass; 1016960 parameters
- **Experimental status:** queued, unmeasured

## 008. Learned vocabulary-logit bias

- **Hypothesis:** A learned vocabulary-logit bias will lower held-out BPB by representing context-independent token frequencies separately from the tied embedding geometry.
- **Mechanism:** The bias supplies a direct per-token prior, allowing the tied embedding matrix to focus more of its limited capacity on contextual representations and token compatibility.
- **Change:** In train.py, add a zero-initialized learned bias to the tied vocabulary output projection and update the enforced parameter count from 1,016,960 to 1,018,752.
- **Expected BPB effect:** -0.012
- **Minimum useful gain:** 0.005
- **Failure signal:** Paired evaluation shows less than 0.005 BPB gain, or the added bias causes instability or a material throughput or memory regression.
- **Resource risk:** Negligible: 1,792 additional parameters and one broadcast addition to logits, with no meaningful expected throughput or peak-memory change.
- **Interaction risk:** Low; it is compatible with most queued architecture and optimizer proposals, but changes output-head state dictionaries and must be reconciled with patches that alter embeddings, logits, or the expected parameter count.
- **Patch:** [`008.patch`](patches/008.patch)
- **Patch SHA-256:** `20a7bc55c1f8c0894131b61c6c6be18f45ba2de4f4cb9699b16975fd7540385a`
- **Static validation:** pass; 1018752 parameters
- **Experimental status:** queued, unmeasured

## 009. Widen MLPs to the parameter cap

- **Hypothesis:** Using the available parameter headroom to widen every feed-forward layer by 6.25% will improve held-out BPB by increasing per-token nonlinear capacity under the fixed training budget.
- **Mechanism:** Each transformer block gains 32 hidden MLP channels, adding 32,768 trainable weights across four blocks and increasing the capacity to store lexical and compositional features without changing attention, optimization, data order, or interfaces.
- **Change:** In train.py, increase ModelConfig.mlp_width from 512 to 544 and update EXPECTED_PARAMETER_COUNT from 1,016,960 to 1,049,728, remaining below the 1,050,000 cap.
- **Expected BPB effect:** -0.012
- **Minimum useful gain:** 0.005
- **Failure signal:** The hypothesis is falsified if paired evaluation shows less than 0.005 BPB gain, or if the candidate breaches throughput, memory, or stability limits.
- **Resource risk:** MLP parameter count and FLOPs increase by 6.25%; total parameters increase by 3.22%. A small throughput reduction and modest activation/optimizer-memory increase are expected. Python compilation passed; runtime inspection could not run because the managed uv cache was inaccessible.
- **Interaction risk:** This independently targets the fixed parent but conflicts structurally with the queued SwiGLU proposal and consumes nearly all parameter headroom needed by other parameter-adding proposals.
- **Patch:** [`009.patch`](patches/009.patch)
- **Patch SHA-256:** `300eeb753839e998addf633109396cf61b5fb0ec1d2559eecd7fb462ef640e97`
- **Static validation:** pass; 1049728 parameters
- **Experimental status:** queued, unmeasured

## 010. Zero-initialized transformer projection biases

- **Hypothesis:** Zero-initialized transformer projection biases will lower held-out BPB by at least 0.002 because this small model benefits from learned affine offsets while retaining the baseline function at initialization.
- **Mechanism:** Attention biases can learn head-specific feature offsets, while MLP biases can shift GELU activation thresholds; zero initialization avoids perturbing initial activations while adding 4,608 trainable parameters.
- **Change:** In train.py, enable zero-initialized biases on every attention QKV/output and MLP input/output linear projection, and update EXPECTED_PARAMETER_COUNT from 1,016,960 to 1,021,568.
- **Expected BPB effect:** -0.004
- **Minimum useful gain:** 0.002
- **Failure signal:** Held-out BPB fails to improve by at least 0.002, or throughput, peak memory, or optimization stability regresses beyond controller limits.
- **Resource risk:** Negligible compute impact; parameters increase by 4,608 while remaining below the cap, with only a small optimizer-state and peak-memory increase.
- **Interaction risk:** The patch is independently applicable to the fixed parent, but combining it with the queued SwiGLU or MLP-width proposals requires recalculating bias dimensions and the expected parameter count.
- **Patch:** [`010.patch`](patches/010.patch)
- **Patch SHA-256:** `84a8e1fc7e87906e38a32fa4330e2ea73b49f8d6d049bb91727209017027d3b5`
- **Static validation:** pass; 1021568 parameters
- **Experimental status:** queued, unmeasured

## 011. Halve the training batch to 32

- **Hypothesis:** At the fixed 20M-token budget, reducing the training batch from 16,384 to 8,192 tokens will improve held-out BPB by providing roughly twice as many parameter updates with less gradient averaging.
- **Mechanism:** The token-indexed cosine schedule is sampled at twice as many update points, allowing AdamW to respond more frequently to training examples while retaining a large 8,192-token batch.
- **Change:** In train.py, change the training CLI default --batch-size from 64 to 32 while preserving evaluation batching, token budgets, learning-rate schedule, architecture, interfaces, and the 1,016,960-parameter count.
- **Expected BPB effect:** -0.015
- **Minimum useful gain:** 0.005
- **Failure signal:** Protected paired full-budget trials show less than 0.005 BPB improvement, or the additional optimizer-step overhead breaches the runtime or throughput gate.
- **Resource risk:** Peak activation memory should decrease, but doubled optimizer-step count may modestly reduce throughput, especially where step overhead dominates.
- **Interaction risk:** Low against the fixed parent; architecture or optimizer changes may shift the optimal batch size and require revalidation.
- **Patch:** [`011.patch`](patches/011.patch)
- **Patch SHA-256:** `5f10b49abd9f1b1dc4d9da4f67bdf34109789b8b20f00d3a8dd0a85572f9b031`
- **Static validation:** pass; 1016960 parameters
- **Experimental status:** queued, unmeasured

## 012. Two wider 64-dimensional attention heads

- **Hypothesis:** At fixed width 128, two 64-dimensional attention heads will lower held-out BPB versus four 32-dimensional heads because TinyStories needs fewer concurrent relation types while each head benefits from a richer query/key/value subspace.
- **Mechanism:** Repartition the unchanged 128 attention channels into two wider heads, increasing per-head representational capacity and halving attention-score planes without changing projection weights or parameter count.
- **Change:** In train.py, change ModelConfig.num_heads from 4 to 2, preserving model width, learned tensor dimensions, interfaces, and the 1,016,960-parameter count.
- **Expected BPB effect:** -0.006
- **Minimum useful gain:** 0.003
- **Failure signal:** Held-out BPB is unchanged or worse, indicating that reduced attention-pattern diversity outweighs the benefit of 64-dimensional heads.
- **Resource risk:** No parameter increase; attention-score memory should decrease and throughput may improve slightly, though backend kernel efficiency for two heads could offset the gain.
- **Interaction risk:** The effect may interact with queued RoPE-base and query-key-normalization proposals because changing head_dim changes rotary frequencies and the dimension normalized per head; this patch remains independently applicable to the fixed parent.
- **Patch:** [`012.patch`](patches/012.patch)
- **Patch SHA-256:** `97ff1fbea01f71df78a649a5f31e111a1e52785511161e0ddb6964aa0c99d730`
- **Static validation:** pass; 1016960 parameters
- **Experimental status:** queued, unmeasured

## 013. Half-dimensional rotary embeddings

- **Hypothesis:** Applying RoPE to only half of each head will lower held-out BPB by at least 0.003 because TinyStories attention benefits from retaining position-invariant query/key channels alongside position-sensitive channels.
- **Mechanism:** Sixteen channels per 32-dimensional head retain rotary relative-position features, while sixteen unrotated channels can learn content similarity without position-dependent rotation, increasing the effective mix of positional and semantic attention features.
- **Change:** In train.py, modify RotaryEmbedding so RoPE rotates the first half of each attention head while leaving the remaining half unrotated; preserve all dimensions, interfaces, and the 1,016,960-parameter count.
- **Expected BPB effect:** -0.006
- **Minimum useful gain:** 0.003
- **Failure signal:** Paired full-budget trials show no BPB improvement, worse BPB, or a material throughput regression relative to the fixed parent.
- **Resource risk:** Parameter count and learned tensor sizes are unchanged. Split and concatenation operations add minor activation traffic and could cause a small throughput regression, with negligible peak-memory impact.
- **Interaction risk:** The effect may depend on the RoPE base and query-key normalization proposals, and it is mutually exclusive with replacing RoPE by learned absolute position embeddings.
- **Patch:** [`013.patch`](patches/013.patch)
- **Patch SHA-256:** `4a2f40a3162db74f4dae1fc42ae42d40e19484c524db4ec5148a665a58203b39`
- **Static validation:** pass; 1016960 parameters
- **Experimental status:** queued, unmeasured

## 014. Cosine learning-rate decay to zero

- **Hypothesis:** Annealing the learning rate fully to zero will improve full-budget held-out BPB by allowing the model to settle into a better local solution during the final portion of training instead of continuing updates at 10% of the peak rate.
- **Mechanism:** The unchanged cosine schedule now progressively suppresses update noise through the end of the fixed token budget, reducing late-stage oscillation around the converged solution.
- **Change:** In train.py, change the training CLI default --minimum-learning-rate from 1e-4 to 0.0 while preserving the cosine schedule, token budgets, architecture, interfaces, and 1,016,960-parameter count.
- **Expected BPB effect:** -0.008
- **Minimum useful gain:** 0.005
- **Failure signal:** Held-out BPB does not improve by at least 0.005, or late-training loss becomes worse as the lower learning rate reduces useful parameter updates.
- **Resource risk:** Negligible; step count, optimizer state, throughput, peak memory, and parameter count are unchanged.
- **Interaction risk:** Low; the change is architecture-independent, but its effect may depend on later accepted changes to the peak learning rate, warmup ratio, batch size, or optimizer.
- **Patch:** [`014.patch`](patches/014.patch)
- **Patch SHA-256:** `8bb9bb6128fa167514f0bf78fcc5ac7a108b159a63d444ddcd5972548bd5cd25`
- **Static validation:** pass; 1016960 parameters
- **Experimental status:** queued, unmeasured

## 015. Training-time logit z-loss

- **Hypothesis:** A small logit z-loss will reduce unnecessary log-partition drift and improve held-out BPB under the fixed training budget.
- **Mechanism:** Penalizing the squared log-sum-exp of valid-token logits discourages poorly conditioned logit offsets while leaving inference unchanged, potentially improving calibration and gradient stability.
- **Change:** In train.py, add a configurable 1e-4 logit z-loss coefficient and augment the training cross-entropy with the mean squared log-partition over non-ignored tokens; preserve the architecture, interfaces, and 1,016,960-parameter count.
- **Expected BPB effect:** -0.004
- **Minimum useful gain:** 0.002
- **Failure signal:** Paired full-budget evaluation shows less than 0.002 BPB improvement, worse BPB, or degraded optimization from excessive logit suppression.
- **Resource risk:** No parameter or persistent-memory increase; one log-sum-exp reduction during training adds a small compute and temporary-memory cost.
- **Interaction risk:** The effect may overlap with future changes to output-logit parameterization, embedding decay, normalization, or vocabulary bias; it is independently applicable to the supplied parent.
- **Patch:** [`015.patch`](patches/015.patch)
- **Patch SHA-256:** `37b1fb3e6c5150933a12a25a001bd631ffd353b3d61f053852f95262a54ba0d0`
- **Static validation:** pass; 1016960 parameters
- **Experimental status:** queued, unmeasured

## 016. Five-layer depth-for-width transformer

- **Hypothesis:** At the fixed full-budget token count, a five-layer transformer with narrower MLPs will achieve lower held-out BPB than the four-layer baseline because additional attention depth improves hierarchical and compositional modeling enough to outweigh reduced per-layer feed-forward width.
- **Mechanism:** The change reallocates capacity from feed-forward width into an additional attention-and-MLP stage, increasing nonlinear and token-mixing depth while using nearly all of the parameter cap. Residual initialization automatically adjusts for five layers.
- **Change:** In train.py, increase ModelConfig.num_layers from 4 to 5, reduce mlp_width from 512 to 384, and update EXPECTED_PARAMETER_COUNT from 1,016,960 to 1,049,984.
- **Expected BPB effect:** -0.008
- **Minimum useful gain:** 0.003
- **Failure signal:** The hypothesis is falsified if paired full-budget evaluation fails to improve held-out BPB by at least 0.003, or if the extra layer violates throughput or memory limits.
- **Resource risk:** Estimated training FLOPs rise about 4%, with modest extra depth-related activation and latency overhead; peak parameters remain within the 1,050,000 cap. Static syntax and analytical parameter-count checks passed, while the permitted inspect command could not run because dependencies were unavailable offline.
- **Interaction risk:** This independently applies to the fixed parent but conflicts structurally with queued MLP-width, SwiGLU, and other parameter-count-changing proposals; those would require recalculating dimensions and the enforced count before combination.
- **Patch:** [`016.patch`](patches/016.patch)
- **Patch SHA-256:** `581a0ecd88253ab4c036da164f9f23dd27bc1587e31f3268680020a0509ee06d`
- **Static validation:** pass; 1049984 parameters
- **Experimental status:** queued, unmeasured

## 017. Shorten learning-rate warmup to 1%

- **Hypothesis:** Reducing warmup from 5% to 1% will lower full-budget held-out BPB because this small pre-normalized transformer can safely reach its maximum learning rate earlier, allocating more of the fixed token budget to productive high-rate optimization.
- **Mechanism:** The token-based schedule reaches 1e-3 after 1% rather than 5% of training tokens, reducing low-learning-rate undertraining at the start while retaining the existing cosine decay over the remaining budget.
- **Change:** In train.py, change the training CLI default --warmup-ratio from 0.05 to 0.01 while preserving the token budget, cosine decay, architecture, interfaces, and 1,016,960-parameter count.
- **Expected BPB effect:** -0.006
- **Minimum useful gain:** 0.003
- **Failure signal:** Held-out BPB fails to improve by at least 0.003, or the shorter warmup causes non-finite loss, gradient clipping spikes, or materially worse early optimization stability.
- **Resource risk:** No parameter or activation-memory change and negligible throughput impact; the only material risk is transient early-training instability from reaching the peak learning rate sooner.
- **Interaction risk:** The effect depends on the maximum learning rate and optimizer dynamics. It is largely orthogonal to the queued minimum-learning-rate change, but may differ with the queued smaller-batch proposal because that changes the number and noise level of optimizer updates.
- **Patch:** [`017.patch`](patches/017.patch)
- **Patch SHA-256:** `5f1caad6b0e7b287766d3d0ad394eafbe75504d0bcd162a7414d401845be094e`
- **Static validation:** pass; 1016960 parameters
- **Experimental status:** queued, unmeasured

## 018. Mask cross-story boundary targets

- **Hypothesis:** Excluding the unpredictable first token of a new story from cross-document training windows will reduce gradient noise and improve full-budget held-out BPB, whose document-wise evaluation does not score those transitions.
- **Mechanism:** Random contiguous windows can span end-of-text separators and currently train the model to predict the unrelated next story's first token. Setting those targets to the existing -100 ignore index aligns training with document-wise evaluation while retaining prediction of the end-of-text token itself.
- **Change:** In train.py, make TokenBatcher optionally mask targets immediately following the end-of-text token, and enable that masking for training batches using the tokenizer's end-of-text ID; preserve evaluation, sampled token order, architecture, interfaces, and the 1,016,960-parameter count.
- **Expected BPB effect:** -0.006
- **Minimum useful gain:** 0.002
- **Failure signal:** Held-out BPB does not improve, or optimization becomes less stable because boundary examples provided useful end-of-text transition regularization.
- **Resource risk:** No parameter or accelerator-memory increase. A vectorized NumPy comparison and masked assignment add negligible CPU work per batch; throughput should be effectively unchanged.
- **Interaction risk:** Largely orthogonal to queued architecture, optimizer, batch-size, and schedule proposals. If the queued logit z-loss proposal is later accepted, its non-ignored-token mask must also exclude these boundary targets; a future document-aware batcher would make this change redundant.
- **Patch:** [`018.patch`](patches/018.patch)
- **Patch SHA-256:** `5cd66984cadf269ff7c7aa772bdfa113f4479c9fb0354c5f5a39e2daab2f1e35`
- **Static validation:** pass; 1016960 parameters
- **Experimental status:** queued, unmeasured

## 019. Double the peak learning rate

- **Hypothesis:** The baseline peak learning rate under-updates the 1M-parameter model within the fixed 20M-token budget; doubling it to 2e-3 will reduce full-budget held-out BPB.
- **Mechanism:** Larger AdamW updates during warmup and the high-learning-rate portion of cosine decay should reach a better solution within the limited optimizer-step budget.
- **Change:** In train.py, change the training CLI default --maximum-learning-rate from 1e-3 to 2e-3; preserve the cosine schedule, token budgets, architecture, interfaces, and 1,016,960-parameter count.
- **Expected BPB effect:** -0.015
- **Minimum useful gain:** 0.005
- **Failure signal:** No held-out BPB improvement, sustained gradient clipping, non-finite values, or materially worse seed variance would falsify the hypothesis.
- **Resource risk:** Parameter count and peak memory are unchanged, and throughput should be unchanged; larger updates introduce a modest stability risk.
- **Interaction risk:** The effect depends on accepted warmup, minimum-learning-rate, and batch-size settings; this fixed-parent patch assumes their baseline values.
- **Patch:** [`019.patch`](patches/019.patch)
- **Patch SHA-256:** `b6dd0993172af1a31e7ac14939dce066673d6b3627a339f313229870793b25dd`
- **Static validation:** pass; 1016960 parameters
- **Experimental status:** queued, unmeasured

## 020. Low-rank untied output correction

- **Hypothesis:** A rank-17 residual vocabulary projection will lower full-budget held-out BPB by at least 0.005 because the tied embedding matrix currently forces input representation and output classification roles to share exactly the same geometry.
- **Mechanism:** The low-rank branch gives the output classifier a learnable task-specific correction while retaining the statistical and parameter-efficiency benefits of tied embeddings. Zero-initializing its final matrix preserves baseline logits at initialization and lets optimization introduce the correction gradually.
- **Change:** In train.py, add a zero-initialized rank-17 residual output adapter (128→17→1,792, bias-free) alongside the tied vocabulary projection, add output_adapter_rank to ModelConfig, and update EXPECTED_PARAMETER_COUNT from 1,016,960 to 1,049,600, remaining below the 1,050,000 cap.
- **Expected BPB effect:** -0.008
- **Minimum useful gain:** 0.005
- **Failure signal:** The candidate produces no held-out BPB reduction, or its approximately 3% extra compute causes a controller resource-gate failure.
- **Resource risk:** Adds 32,640 parameters and roughly 3% model FLOPs per token; optimizer-state and parameter memory rise by about 3.2%, while activation-memory impact should be negligible because the vocabulary-sized logits already exist.
- **Interaction risk:** Conflicts with accepted parameter-adding patches near the cap and requires checkpoint/config adaptation; it may overlap partially with a vocabulary-logit bias but is otherwise independent of the queued proposals.
- **Patch:** [`020.patch`](patches/020.patch)
- **Patch SHA-256:** `9c7caaaa3fec58f42e39dc9662694214b461b3f07e049bdefa9c2949697c40cf`
- **Static validation:** pass; 1049600 parameters
- **Experimental status:** queued, unmeasured

## 021. Causal depthwise convolution in each MLP

- **Hypothesis:** Giving each MLP an independently learned three-token causal filter will improve held-out BPB by at least 0.005 by capturing short-range patterns more directly than attention alone.
- **Mechanism:** The depthwise convolution mixes each nonlinear MLP feature with its two preceding positions before channel projection. Identity initialization makes the initial function match the parent while training can learn inexpensive feature-specific local filters.
- **Change:** In train.py, insert an identity-initialized causal depthwise 1D convolution with kernel size 3 between GELU and the output projection of every Transformer MLP, and update EXPECTED_PARAMETER_COUNT from 1,016,960 to 1,023,104.
- **Expected BPB effect:** -0.008
- **Minimum useful gain:** 0.005
- **Failure signal:** The paired full-budget candidate improves BPB by less than 0.005, degrades BPB, or exceeds controller throughput or memory limits.
- **Resource risk:** Adds 6,144 parameters and a grouped convolution over MLP activations. Peak memory should rise only slightly, but grouped-convolution kernel efficiency may reduce throughput, especially on CPU or MPS.
- **Interaction risk:** This changes the MLP layout and parameter count, so proposals that replace, widen, or resize MLPs—especially SwiGLU, 544-wide MLPs, and the five-layer depth-for-width model—would require adaptation rather than direct composition.
- **Patch:** [`021.patch`](patches/021.patch)
- **Patch SHA-256:** `9d4c2554af031205fc26ea9a7356b85498b7ff893072fdcd5987431e4b111730`
- **Static validation:** pass; 1023104 parameters
- **Experimental status:** queued, unmeasured

## 022. Final-20% cosine decay

- **Hypothesis:** Keeping the learning rate at 1e-3 through the middle 75% of these short fixed-token runs will reduce under-optimization and lower full-budget held-out BPB relative to immediately beginning cosine decay after warmup.
- **Mechanism:** A warmup-stable-decay schedule supplies larger parameter updates during most of the finite training budget while retaining a low-learning-rate tail for final convergence.
- **Change:** In train.py, add a --decay-ratio training option defaulting to 0.2 and modify the learning-rate schedule to hold the maximum rate after the existing 5% warmup until 80% of the token budget, then cosine-decay to the existing minimum rate over the final 20%; preserve architecture, token budgets, and the 1,016,960-parameter count.
- **Expected BPB effect:** -0.015
- **Minimum useful gain:** 0.005
- **Failure signal:** Held-out BPB does not improve by at least 0.005, or the longer high-learning-rate plateau causes non-finite values, substantially increased clipping, or worse late-training loss.
- **Resource risk:** No parameter or activation-memory increase and negligible schedule-computation overhead; the main risk is optimization instability or overshooting from the higher average learning rate.
- **Interaction risk:** Strong interaction with queued learning-rate proposals that change the peak, floor, or warmup, especially proposals 14, 17, and 19; it should be measured independently against the fixed parent.
- **Patch:** [`022.patch`](patches/022.patch)
- **Patch SHA-256:** `285c88cb024b14bca25dc9f28f042597aa128d982a2caf6445b75146bf9fa12d`
- **Static validation:** pass; 1016960 parameters
- **Experimental status:** queued, unmeasured

## 023. Learnable per-head attention temperatures

- **Hypothesis:** Allowing every attention head to learn its own softmax temperature will lower full-budget held-out BPB by at least 0.003 because heads serving different syntactic and narrative roles benefit from different attention sharpness.
- **Mechanism:** Exponentiated zero-initialized scalars preserve the baseline function at initialization, then let each layer and head independently sharpen or flatten its attention distribution without changing projections or head dimensions.
- **Change:** In train.py, add one zero-initialized learned log attention-scale per head in each attention layer, multiply each head's rotary-encoded query by exp(scale) before scaled dot-product attention, and update EXPECTED_PARAMETER_COUNT from 1,016,960 to 1,016,976.
- **Expected BPB effect:** -0.006
- **Minimum useful gain:** 0.003
- **Failure signal:** The hypothesis is falsified if held-out BPB fails to improve by at least 0.003, training becomes unstable, or the learned scales remain effectively zero across heads.
- **Resource risk:** Adds 16 trainable parameters and one broadcast exponential and multiplication per attention layer; peak-memory and throughput impact should be negligible, though unbounded scales could cause overly sharp attention or unstable gradients.
- **Interaction risk:** Query-key RMS normalization would make these explicit temperatures more influential, while changing the head count would alter their number; otherwise the patch is independent of the queued activation, schedule, and positional-encoding proposals.
- **Patch:** [`023.patch`](patches/023.patch)
- **Patch SHA-256:** `1cbb2891067616de8972c1a9799fe8fedbfb4758b897a233887124fd0b525174`
- **Static validation:** pass; 1016976 parameters
- **Experimental status:** queued, unmeasured

## 024. Parameter-neutral multi-query attention reallocation

- **Hypothesis:** At this scale, distinct key/value heads are more redundant than additional MLP capacity, so parameter-neutral multi-query attention with wider MLPs will reduce full-budget held-out BPB by at least 0.004.
- **Mechanism:** Sharing keys and values saves 24,576 weights per block, which are reallocated exactly to 96 additional MLP channels per block while retaining four independently projected query heads.
- **Change:** In train.py, add ModelConfig.num_kv_heads=1, project one shared key/value head and broadcast it across four query heads, and increase mlp_width from 512 to 608. Preserve the 1,016,960-parameter count.
- **Expected BPB effect:** -0.008
- **Minimum useful gain:** 0.004
- **Failure signal:** Held-out BPB improves by less than 0.004, training becomes unstable, or throughput or peak memory breaches controller limits.
- **Resource risk:** Parameter count is unchanged and projection/MLP FLOPs are approximately reallocated; expanded key/value views may cause a small attention-kernel throughput or temporary-memory regression.
- **Interaction risk:** Structurally interacts with queued head-count, MLP-width/type, and depth changes; parameter allocation would need recalculation if one is accepted first.
- **Patch:** [`024.patch`](patches/024.patch)
- **Patch SHA-256:** `c61d3df50e374fd993f0e44719ac457a69638d844a4a0e157d316df3074ed08c`
- **Static validation:** pass; 1016960 parameters
- **Experimental status:** queued, unmeasured

## 025. Per-channel residual branch scales

- **Hypothesis:** Allowing each block to independently calibrate the per-channel strength of its attention and MLP residual updates will reduce held-out BPB by at least 0.002 at the full budget versus the fixed-parent baseline.
- **Mechanism:** Unit initialization preserves the baseline function exactly at initialization, while learned per-channel LayerScale vectors let optimization suppress noisy branch features and amplify useful ones without changing attention, MLP dimensions, or data order.
- **Change:** In train.py, add unit-initialized learned 128-channel residual scales to the attention and MLP branches of every Transformer block, apply them immediately before each residual addition, and update EXPECTED_PARAMETER_COUNT from 1,016,960 to 1,017,984.
- **Expected BPB effect:** -0.004
- **Minimum useful gain:** 0.002
- **Failure signal:** The paired full-budget BPB gain is below 0.002, or the added elementwise scaling causes a material throughput, memory, or stability regression.
- **Resource risk:** Low: adds 1,024 trainable parameters plus optimizer state and two inexpensive broadcast elementwise multiplications per block; peak memory and throughput impact should be negligible.
- **Interaction risk:** The scales are independently applicable to the fixed parent, but their benefit may change if residual-stream RMSNorm, SwiGLU, projection biases, altered depth, or other residual-magnitude changes are accepted first.
- **Patch:** [`025.patch`](patches/025.patch)
- **Patch SHA-256:** `86a6a7a9cd74b92cb31fefc1a5caef3e3a9221b371d0fb64daca9cfa20938e49`
- **Static validation:** pass; 1017984 parameters
- **Experimental status:** queued, unmeasured

## 026. Per-head learnable RoPE frequencies

- **Hypothesis:** Allowing each attention head and layer to adapt its initially baseline-equivalent RoPE frequencies will improve held-out BPB by specializing positional timescales to TinyStories' short-context structure.
- **Mechanism:** Each head learns multiplicative frequency adjustments in log space while retaining RoPE's relative-position structure. Zero initialization makes the starting model exactly equivalent to the parent, while 256 added scalars permit layer- and head-specific positional specialization.
- **Change:** In train.py, make RoPE frequencies independently learnable for every attention head and layer by adding zero-initialized log-frequency scales, preserving the baseline initialization, dimensions, and interfaces; update EXPECTED_PARAMETER_COUNT from 1,016,960 to 1,017,216.
- **Expected BPB effect:** -0.006
- **Minimum useful gain:** 0.002
- **Failure signal:** The hypothesis is falsified if paired full-budget evaluation shows less than 0.002 BPB gain, frequency parameters become non-finite, or throughput or memory exceeds controller limits.
- **Resource risk:** Adds 256 parameters and only a small per-forward exponentiation over 64 values per layer; expected throughput and peak-memory impact are negligible. Static compilation passed, while train.py inspect could not run because the isolated environment lacked cached dependencies.
- **Interaction risk:** Directly overlaps RoPE-base and partial-RoPE proposals and is incompatible with replacing RoPE with absolute embeddings; it should be rebased or re-evaluated if any positional-encoding patch is accepted.
- **Patch:** [`026.patch`](patches/026.patch)
- **Patch SHA-256:** `354c9737b70fcc9221d750404bfe3401bd5bee1658879cfe3d6db6d3fd346144`
- **Static validation:** pass; 1017216 parameters
- **Experimental status:** queued, unmeasured

## 027. Reduce AdamW weight decay to 0.01

- **Hypothesis:** Because the fixed 20M-token budget leaves this 1M-parameter model undertrained, reducing AdamW weight decay from 0.1 to 0.01 will avoid excessive shrinkage of learned matrices and lower held-out BPB by at least 0.003.
- **Mechanism:** Tenfold weaker decoupled weight decay lets attention, MLP, and tied embedding weights retain more task-relevant magnitude during the short fixed-budget optimization while keeping modest regularization on matrix parameters.
- **Change:** In train.py, change the training CLI default --weight-decay from 0.1 to 0.01 while preserving optimizer grouping, architecture, interfaces, token budgets, and the 1,016,960-parameter count.
- **Expected BPB effect:** -0.006
- **Minimum useful gain:** 0.003
- **Failure signal:** The full-budget paired trial improves held-out BPB by less than 0.003, is neutral or worse, or exhibits a larger train-to-dev generalization gap consistent with insufficient regularization.
- **Resource risk:** Negligible throughput and peak-memory impact; the optimizer operations, state size, parameter count, and training schedule are unchanged.
- **Interaction risk:** The effect may weaken if the queued no-decay-on-tied-embeddings proposal is accepted, because that removes weight decay from the model's largest shared matrix; it may also depend on accepted learning-rate or batch-size changes.
- **Patch:** [`027.patch`](patches/027.patch)
- **Patch SHA-256:** `52f270c732a2863b80ec782dcd1c0e9f31b92e1390a803288b97fb77320d4e63`
- **Static validation:** pass; 1016960 parameters
- **Experimental status:** queued, unmeasured

## 028. Learned global logit temperature

- **Hypothesis:** A directly learned global logit temperature will improve held-out BPB by at least 0.003 by calibrating prediction sharpness independently of the tied embedding matrix.
- **Mechanism:** The scalar supplies a low-cost calibration degree of freedom, allowing cross-entropy to adjust overall softmax confidence without changing token geometry or globally rescaling the tied input/output embeddings.
- **Change:** In train.py, add a zero-initialized learned scalar logit_scale, multiply vocabulary logits by exp(logit_scale), and update EXPECTED_PARAMETER_COUNT from 1,016,960 to 1,016,961.
- **Expected BPB effect:** -0.004
- **Minimum useful gain:** 0.003
- **Failure signal:** Paired full-budget held-out BPB improves by less than 0.003, regresses, or the learned scale remains effectively 1.0 throughout training.
- **Resource risk:** Negligible throughput and memory impact: one trainable scalar, one exponential per forward pass, and one elementwise logit multiplication.
- **Interaction risk:** The effect may overlap with queued output-bias, low-rank output-adapter, embedding weight-decay, or z-loss proposals; it is independently applicable to the fixed parent.
- **Patch:** [`028.patch`](patches/028.patch)
- **Patch SHA-256:** `502625e382e08a0c0bbf85e408045210f3ce43f5702ce2326328cfe2f89de23f`
- **Static validation:** pass; 1016961 parameters
- **Experimental status:** queued, unmeasured

## 029. Causal depthwise convolution on attention values

- **Hypothesis:** Giving each attention value channel a learnable causal three-token receptive field will improve held-out BPB by exposing reusable local phrase features while preserving the baseline function at initialization.
- **Mechanism:** The identity-initialized convolution initially copies each value unchanged, then can learn channel-specific mixtures of the current and two preceding value vectors before global causal attention routes them.
- **Change:** In train.py, add an identity-initialized, bias-free, kernel-size-3 causal depthwise convolution to each attention layer's value projection before splitting heads, and update EXPECTED_PARAMETER_COUNT from 1,016,960 to 1,018,496.
- **Expected BPB effect:** -0.008
- **Minimum useful gain:** 0.005
- **Failure signal:** The hypothesis is falsified if paired evaluation shows less than 0.005 BPB improvement, instability, or a controller-rejected throughput or memory regression.
- **Resource risk:** Adds 1,536 parameters and one small grouped convolution per layer. Peak memory should change minimally, but convolution launches and transposes may modestly reduce throughput.
- **Interaction risk:** This may overlap with queued proposal 21's MLP depthwise convolution because both add local token mixing; effects may not combine additively if that proposal is accepted.
- **Patch:** [`029.patch`](patches/029.patch)
- **Patch SHA-256:** `b06bed7ceb1b34d23681b3c9ff6d8918066e36ac7af7c445967c6ddcee93aa70`
- **Static validation:** pass; 1018496 parameters
- **Experimental status:** queued, unmeasured

## 030. Fully affine LayerNorm

- **Hypothesis:** Learned LayerNorm offsets will lower held-out BPB by at least 0.002 because the bias-free projections currently cannot independently learn normalized activation mean shifts.
- **Mechanism:** Zero-initialized biases preserve the parent's initial function while allowing each pre-normalized residual branch and the final normalized representation to learn channel-wise offsets, increasing representational flexibility with minimal parameters.
- **Change:** In train.py, replace the learned-scale-only LayerNorms before attention, before the MLP, and after the final block with fully affine LayerNorms by adding zero-initialized 128-channel biases; update EXPECTED_PARAMETER_COUNT from 1,016,960 to 1,018,112.
- **Expected BPB effect:** -0.004
- **Minimum useful gain:** 0.002
- **Failure signal:** The paired full-budget candidate shows less than 0.002 BPB gain, no gain, or worse BPB relative to the parent.
- **Resource risk:** Adds 1,152 parameters (about 0.11%) while remaining below the cap; throughput and peak-memory impact should be negligible.
- **Interaction risk:** The effect may be redundant with projection-bias or vocabulary-logit-bias proposals, and it conflicts structurally with replacing LayerNorm by RMSNorm.
- **Patch:** [`030.patch`](patches/030.patch)
- **Patch SHA-256:** `11c9cb890e220d73b9276066e70da7aa734958d648713974273c900973441016`
- **Static validation:** pass; 1018112 parameters
- **Experimental status:** queued, unmeasured
