diff --git a/train.py b/train.py
index a3838f6..da24392 100644
--- a/train.py
+++ b/train.py
@@ -32,7 +32,7 @@ from autodidact.data.config import END_OF_TEXT_TOKEN, default_output_root
 from autodidact.data.integrity import verify_dataset
 from autodidact.data.reader import PreparedSplit
 
-EXPECTED_PARAMETER_COUNT = 1_016_960
+EXPECTED_PARAMETER_COUNT = 1_049_600
 MAX_PARAMETER_COUNT = 1_050_000
 CHECKPOINT_SCHEMA_VERSION = 2
 METRICS_SCHEMA_VERSION = 1
@@ -52,6 +52,7 @@ class ModelConfig:
     model_width: int = 128
     num_heads: int = 4
     mlp_width: int = 512
+    output_adapter_rank: int = 17
     rope_base: float = 10_000.0
     layer_norm_epsilon: float = 1e-5
     dropout: float = 0.0
@@ -69,6 +70,8 @@ class ModelConfig:
             raise ValueError("context_length must be positive")
         if self.vocab_size <= 0:
             raise ValueError("vocab_size must be positive")
+        if self.output_adapter_rank <= 0:
+            raise ValueError("output_adapter_rank must be positive")
         if not 0.0 <= self.dropout < 1.0:
             raise ValueError("dropout must be in [0, 1)")
 
@@ -234,9 +237,14 @@ class TransformerLM(nn.Module):
             config.layer_norm_epsilon,
         )
         self.output = nn.Linear(config.model_width, config.vocab_size, bias=False)
+        self.output_adapter = nn.Sequential(
+            nn.Linear(config.model_width, config.output_adapter_rank, bias=False),
+            nn.Linear(config.output_adapter_rank, config.vocab_size, bias=False),
+        )
 
         self.apply(self._initialize_weights)
         self.output.weight = self.token_embedding.weight
+        nn.init.zeros_(self.output_adapter[-1].weight)
         residual_std = 0.02 / math.sqrt(2 * config.num_layers)
         for block in self.blocks:
             nn.init.normal_(block.attention.output.weight, mean=0.0, std=residual_std)
@@ -262,7 +270,8 @@ class TransformerLM(nn.Module):
         hidden = self.token_embedding(token_ids)
         for block in self.blocks:
             hidden = block(hidden)
-        logits = self.output(self.final_norm(hidden))
+        hidden = self.final_norm(hidden)
+        logits = self.output(hidden) + self.output_adapter(hidden)
         loss = None
         if targets is not None:
             if targets.shape != token_ids.shape:
