diff --git a/train.py b/train.py
index a3838f6..736b7fc 100644
--- a/train.py
+++ b/train.py
@@ -55,6 +55,7 @@ class ModelConfig:
     rope_base: float = 10_000.0
     layer_norm_epsilon: float = 1e-5
     dropout: float = 0.0
+    embedding_dropout: float = 0.1
 
     @property
     def head_dim(self) -> int:
@@ -71,6 +72,8 @@ class ModelConfig:
             raise ValueError("vocab_size must be positive")
         if not 0.0 <= self.dropout < 1.0:
             raise ValueError("dropout must be in [0, 1)")
+        if not 0.0 <= self.embedding_dropout < 1.0:
+            raise ValueError("embedding_dropout must be in [0, 1)")
 
 
 @dataclass(frozen=True)
@@ -228,6 +231,7 @@ class TransformerLM(nn.Module):
         config.validate()
         self.config = config
         self.token_embedding = nn.Embedding(config.vocab_size, config.model_width)
+        self.embedding_dropout = nn.Dropout(config.embedding_dropout)
         self.blocks = nn.ModuleList(TransformerBlock(config) for _ in range(config.num_layers))
         self.final_norm = WeightOnlyLayerNorm(
             config.model_width,
@@ -259,7 +263,7 @@ class TransformerLM(nn.Module):
                 f"sequence length {token_ids.size(1)} exceeds "
                 f"context length {self.config.context_length}"
             )
-        hidden = self.token_embedding(token_ids)
+        hidden = self.embedding_dropout(self.token_embedding(token_ids))
         for block in self.blocks:
             hidden = block(hidden)
         logits = self.output(self.final_norm(hidden))
