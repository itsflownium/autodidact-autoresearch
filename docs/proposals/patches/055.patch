diff --git a/train.py b/train.py
index a3838f6..08beac7 100644
--- a/train.py
+++ b/train.py
@@ -32,7 +32,7 @@ from autodidact.data.config import END_OF_TEXT_TOKEN, default_output_root
 from autodidact.data.integrity import verify_dataset
 from autodidact.data.reader import PreparedSplit
 
-EXPECTED_PARAMETER_COUNT = 1_016_960
+EXPECTED_PARAMETER_COUNT = 1_017_344
 MAX_PARAMETER_COUNT = 1_050_000
 CHECKPOINT_SCHEMA_VERSION = 2
 METRICS_SCHEMA_VERSION = 1
@@ -229,6 +229,9 @@ class TransformerLM(nn.Module):
         self.config = config
         self.token_embedding = nn.Embedding(config.vocab_size, config.model_width)
         self.blocks = nn.ModuleList(TransformerBlock(config) for _ in range(config.num_layers))
+        self.intermediate_mix = nn.Parameter(
+            torch.zeros(config.num_layers - 1, config.model_width)
+        )
         self.final_norm = WeightOnlyLayerNorm(
             config.model_width,
             config.layer_norm_epsilon,
@@ -260,8 +263,17 @@ class TransformerLM(nn.Module):
                 f"context length {self.config.context_length}"
             )
         hidden = self.token_embedding(token_ids)
+        intermediate_states = []
         for block in self.blocks:
             hidden = block(hidden)
+            intermediate_states.append(hidden)
+        final_hidden = hidden
+        for mix, intermediate in zip(
+            self.intermediate_mix,
+            intermediate_states[:-1],
+            strict=True,
+        ):
+            hidden = hidden + mix * (intermediate - final_hidden)
         logits = self.output(self.final_norm(hidden))
         loss = None
         if targets is not None:
