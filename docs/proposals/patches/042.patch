diff --git a/train.py b/train.py
index a3838f6..15d3756 100644
--- a/train.py
+++ b/train.py
@@ -217,8 +217,9 @@ class TransformerBlock(nn.Module):
         self.dropout = nn.Dropout(config.dropout)
 
     def forward(self, inputs: torch.Tensor) -> torch.Tensor:
-        inputs = inputs + self.dropout(self.attention(self.attention_norm(inputs)))
-        return inputs + self.dropout(self.mlp(self.mlp_norm(inputs)))
+        attention = self.attention(self.attention_norm(inputs))
+        mlp = self.mlp(self.mlp_norm(inputs))
+        return inputs + self.dropout(attention) + self.dropout(mlp)
 
 
 class TransformerLM(nn.Module):
