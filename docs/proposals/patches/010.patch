diff --git a/train.py b/train.py
index a3838f6..90ea325 100644
--- a/train.py
+++ b/train.py
@@ -32,7 +32,7 @@ from autodidact.data.config import END_OF_TEXT_TOKEN, default_output_root
 from autodidact.data.integrity import verify_dataset
 from autodidact.data.reader import PreparedSplit
 
-EXPECTED_PARAMETER_COUNT = 1_016_960
+EXPECTED_PARAMETER_COUNT = 1_021_568
 MAX_PARAMETER_COUNT = 1_050_000
 CHECKPOINT_SCHEMA_VERSION = 2
 METRICS_SCHEMA_VERSION = 1
@@ -159,9 +159,9 @@ class CausalSelfAttention(nn.Module):
         self.query_key_value = nn.Linear(
             config.model_width,
             3 * config.model_width,
-            bias=False,
+            bias=True,
         )
-        self.output = nn.Linear(config.model_width, config.model_width, bias=False)
+        self.output = nn.Linear(config.model_width, config.model_width, bias=True)
         self.rope = RotaryEmbedding(config.head_dim, config.rope_base)
 
     def forward(self, inputs: torch.Tensor) -> torch.Tensor:
@@ -210,9 +210,9 @@ class TransformerBlock(nn.Module):
             config.layer_norm_epsilon,
         )
         self.mlp = nn.Sequential(
-            nn.Linear(config.model_width, config.mlp_width, bias=False),
+            nn.Linear(config.model_width, config.mlp_width, bias=True),
             nn.GELU(),
-            nn.Linear(config.mlp_width, config.model_width, bias=False),
+            nn.Linear(config.mlp_width, config.model_width, bias=True),
         )
         self.dropout = nn.Dropout(config.dropout)
 
@@ -246,6 +246,8 @@ class TransformerLM(nn.Module):
     def _initialize_weights(module: nn.Module) -> None:
         if isinstance(module, (nn.Linear, nn.Embedding)):
             nn.init.normal_(module.weight, mean=0.0, std=0.02)
+        if isinstance(module, nn.Linear) and module.bias is not None:
+            nn.init.zeros_(module.bias)
 
     def forward(
         self,
